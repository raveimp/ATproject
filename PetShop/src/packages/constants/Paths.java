package packages.constants;

import java.io.File;
public class Paths {
    public static final String RESOURCES = "src" + File.separator + "resources" + "json";
    public static final String HTTP_REQUEST = RESOURCES + File.separator + "json";
}